<<<<<<< SEARCH
            <tr
                v-for="row in table.getRowModel().rows"
                :key="row.id"
                class="cursor-pointer hover:bg-primary/25"
                @click="
                    () => {
                        selectedCourse = props.courses[row.index];
                        showCourseDetailsModal = true;
                    }
                "
            >
=======
            <tr
                v-for="row in table.getRowModel().rows"
                :key="row.id"
                class="cursor-pointer hover:bg-primary/25 focus-within:bg-primary/25 focus:outline-none focus:ring-2 focus:ring-primary focus:ring-inset"
                tabindex="0"
                role="button"
                @click="
                    () => {
                        selectedCourse = props.courses[row.index];
                        showCourseDetailsModal = true;
                    }
                "
                @keydown.enter="
                    () => {
                        selectedCourse = props.courses[row.index];
                        showCourseDetailsModal = true;
                    }
                "
                @keydown.space.prevent="
                    () => {
                        selectedCourse = props.courses[row.index];
                        showCourseDetailsModal = true;
                    }
                "
            >
>>>>>>> REPLACE
